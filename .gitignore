# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Local-only editor / tool settings (contain machine-specific absolute paths)
.claude/settings.local.json
.vscode/
.idea/

# Dependencies & build output (this project has no build step, but just in case)
node_modules/
dist/
build/

# Environment / secrets — never commit these.
# Supabase Edge Function secrets live in `supabase secrets set`, not in the repo.
.env
.env.*
!.env.example
*.pem
*.key

# Logs & scratch
*.log
tmp/
scratch/

# Nested demo build artifacts / caches
mkr-owner-game-demo/node_modules/
mkr-owner-game-demo/dist/
mkr-owner-game-demo/.vinext/
mkr-owner-game-demo/.wrangler/
mkr-owner-game-demo/.next/
