# 变更清单

从 `v1-before-update` 到现在，14 次提交。回滚锚点：

| 标签 | 指向 | 用途 |
|---|---|---|
| `v1-before-update` | `55118a5` | 货架/厨房/平面图三个视图之前 |
| `v2-before-packsize` | `30f6830` | 供应商包装规格之前 |

```
git checkout v1-before-update -- .   # 把工作区还原到某个标签的内容
```

---

## 你提的 12 项

### 1. 送货需要照相上传 ✅

照片上传本来就有，是可选的。改成**有问题的行必须配照片** —— 标了短交/损坏/错货就必须拍，正常收货仍然可选。

理由：没照片的索赔供应商不认，而等到有人想起来去要的时候，箱子早扔了。一张干净的送货单不需要照片，强制反而会让人想办法绕过。

提交时会二次校验，不能在最后一刻改条件绕过。

### 2. 库存与成本加订货单 PDF / 链接 ✅

- **打印/PDF** —— 用浏览器自带的打印对话框，不引入 PDF 库
- **分享链接** —— `order.html` 独立页面，供应商无需账号即可打开

订单数据编码在 URL fragment 里。浏览器**不会**把 fragment 发给服务器，所以不需要后端，也没有可泄露、可过期的存储。代价写在页面上：拿到链接的人都能看到这张订单。链接里有数量和你的店名电话，**没有价格** —— 订货是「给我这些」，不是把你的成本告诉供应商。

顺带修掉一个逻辑错误：订货数量以前不取整，会让菜商送「6.14 个篮子」。现在按整包向上取整，底下的重量也按取整后的包数重算（不会出现「7 篮 / 18.42 kg」这种自相矛盾），预估金额、CSV、送到后门的行全部跟着改。

### 3. 顾客投诉表 ✅

反馈页拆成两个标签：**投诉**（今天要处理的）和**评价**（事后打分的）。合在一起会让需要立刻行动的那条被埋掉。

投诉表按你给的纸质表实现：姓名、联系方式、事发时间、经过、经办员工、关怀问句、手写签名。三级处理办法**可编辑**，存在 kitchen 记录上，别的店也能用。Level 3 自动触发红色警报。

⚠️ **你那张表自相矛盾**：开头写「will not refund you money」，但三级处理里全都写了退款。我按三级为准实现（那是柜台实际执行的部分），开头那段做成可编辑，政策页里也标注了这一点，你自己决定怎么统一。

### 4. 审计日志去敏感 ✅

清掉 POS/薪资时代的死标签：`order.refund`、`pay.blinddrop`、`labor.approve`、`member.topup`、`coupon.issue` —— 全是已删除模块留下的，它们描述的是一个会读营业额、会算工资的系统。

同时补上**真正在用却没标签**的动作：`stock.purchase`、`delivery.confirm` 这些最常见的以前直接把原始字符串显示给老板看。现在 27 个标签对 27 个在用动作，无死标签无缺口。

`tfn.view` 改名 `id.view`（后来因为第 6 项又加回了真正的 `tfn.view`）。查看记录**保留** —— 知道谁看过谁的证件是一种保护 —— 但只记谁看了谁，不记内容。

### 5. 老板要看到任务、也能添加员工 ✅

老板导航加「每日任务」和「添加人员」。两个页面**复用 manager 模块的实现**，不复制代码 —— 它们写的表各有规则，第二份实现慢慢跑偏，就会出现两个门户对员工记录要求不一致。

仍可按店关闭，员工依然不能加人。

### 6. 员工自己填 TFN、签证 ✅

加密和 RLS 的基础设施本来就在（早期做过又拿掉了），这次补的是录入路径，并按澳洲 TFN 的敏感度加了防护：

- **员工自己填**，没人替别人填
- **可以不提供** —— 澳洲法律上本就不强制，勾「我不想提供」照样完成入职。只接受数字的表单会逼人乱填一个
- **ATO 校验位**挡掉打错的数字，也挡掉 `000000000` 这种能被 11 整除但明显是乱填的
- **本人看也打码**成 `••• ••• 782`，手机放操作台上不会被瞄到
- **老板揭示要点按钮**，每次写审计行 —— 只记谁看了谁，不记号码
- **不进任何导出**

签证只存状态不做判断：不查签证条件，不算工时。选公民/PR 会清掉签证字段，不留过期的 subclass。

README 原来写着「不收集 TFN 和签证数据」，跟代码矛盾，已改。

### 7. Logo 自动取色换主题 ✅

按色相分桶、用饱和度和不透明度加权，忽略接近纸色和墨色的像素（logo 大部分是背景和描边，直接投票它们会赢）。

**只改明度，不改色相饱和度**，压到两个方向都过 WCAG AA 4.5:1：白字压在上面、以及它自己当文字压在页面上。设置页显示两个实际对比度数值，并标注是否被调暗过。

亮黄 logo `#FCDA01` → `#796A0A`。三个色值都能手动改，有「重新从 logo 读取」和「恢复默认」。

### 8. 云端有没有统一 ✅（发现是 bug）

**没有统一，而且比预想严重。**

第一层：`claims`、`waste`、`reconciliations` 三张表本地在用但不在同步列表里。

第二层（在你的浏览器控制台里发现的）：`suppliers`、`purchases`、`stocktakes`、`deliveries`、`sops`、`trainings` 在 Supabase 里**根本不存在** —— `stock-training-setup.sql` 从来没跑过。整条供应链只存在本地浏览器里，换设备或清缓存就没了。

已修：同步列表补齐，SQL 建表脚本补上缺的两张表并已在你的项目跑过（8 张表全部确认存在）。

加了防复发检查：写了新表但忘记加进同步列表，控制台立刻警告。

顺带修掉一段危险注释 —— `reconciliations` 被标成「已废弃，可以 drop」，但代码还在用它做供应商对账，照做会删真数据。

### 9. 停电停网自动上传 ✅（本来就有）

`js/net.js` 断网进离线队列，恢复自动 flush 并提示同步完成。这项不需要改。

### 10. 分店做成一个店、老板可切换 ✅

切换本来只能在 Branches 页做。现在切换器**固定在侧边栏**，每一页都能切，而且切完**停在同一页面**而不是跳回首页 —— 你切换的理由通常就是想看另一家店的这一页。

每家分店有独立的品牌色和功能开关，切换时整个界面跟着换。示例数据加了第二家店（Southbank），否则切换器根本不会出现。

### 11. 库存分类 ✅

Meat / Veg / Dry store / Packaging，跟易腐/不易腐两套并存互不替代（后者是 app 判断保质期的规则，前者是老板自己的货架说法）。

分类列表存在 kitchen 记录上而不是从商品推导 —— 分类要能先存在再装东西，而且顺序是老板排的。改名会带着商品走，删分类只把商品退回 Unfiled，**不删库存**。指向已删分类的商品仍然给它一个标签，否则会变成点不到的孤儿。

子标签栏带数量，支持勾选批量移动，item 编辑器可内联新建分类。

### 12. 四个测试账号 ✅

`test_boss1` / `test_manager1` / `test_staff1` / `test_staff2`，跟出现在截图里的 demo 五人组分开。

`MKR.setup.createTestAccounts()` 用公开 anon key 建 Auth 用户（不经手 service key），写 `users` 记录，并打印 `profiles` 的 SQL 给你在 SQL Editor 跑。

两半都必要：`profiles` 是角色的服务端真相来源且受 RLS 保护，只能从 SQL Editor 写；`users` 是排班、任务、团队页读的 app 侧记录。只建 Auth 用户的话，人能登进去但门户里查无此人。

⚠️ 四个账号共用一个好猜的密码，其中一个是 **owner**。测试店无所谓，项目开始装真实数据前请去 Supabase 删掉。

---

## 你没提但确实是 bug 的

**供应商包装规格完全不存在** —— 餐厅按公斤盘点番茄，供应商按篮卖，送货单只写篮价。把篮价填进「每公斤」字段，库存会加 3 而不是 9，价格页会拿 $21.60 跟上周的 $7.20 比，得出一个看起来很合理的错误结论。现在 item 可设包装规格，后门按箱收货，换算单向（散买不会显示成「2.67 篮」），行金额用纸上的算术免得跟发票差几分钱。

**登录页角色标签会覆盖你填的用户名** —— 点「老板」会把输入框改成演示账号 `boss` 并清空密码。你就是被这个卡住的：填了 `test_boss1`，点了下角色标签，变成用 `boss` 配 `threepandas` 登录。现在只在输入框为空时才自动填。

**登录和开机都在等云端全量拉取** —— 这个 app 号称 local-first，但两条路径都把 24 张表拉完才放行，其中通知权限框还会一直卡到你点为止。改成给云端 3 秒领先，超时先进去、后台补完；通知权限改到进界面之后再问。

**`--accent-grad` 写死橙色渐变** —— 主按钮和登录 logo 根本不跟品牌走，只有标签和头像变色。

**`brand.load()` 只在「恢复已有会话」分支调用** —— 新登录和预览模式都不加载，第一次看到 app 的人反而看到默认橙色。

**签名板两个 bug** —— 按一下不画东西也会存空白 PNG 记成「已签名」（改成按像素判断）；canvas 在弹窗动画未完成时定尺寸，量到 0 宽，笔画画到 1px 画布外（改成四重适配）。

**`stock-view.js` 里一个 NUL 字节** —— 分类哨兵值本想写前导空格，实际写成 `\x00`。JS 照跑所以测试全过，但文件被当二进制，`grep` 从此静默搜不到这个文件。

---

## 判断错、后来纠正的

**par level（应有存量）** —— 我说它完全不存在、建议优先做。错的：`fullOf()` 已经用「日均用量 ×（备货天数+7）」定义满罐，`safety` 作为补货线画在杯子上。这条没做。

**`.btn-accent` 没有背景** —— 我看到 `backgroundColor` 是透明就下了结论，其实渐变在 `background-image` 上，读错了属性。按钮一直正常。

**签名板宽度为 0 是 CSS bug** —— 实际是测试环境视口本身就是 0×0。不过那次改动仍然值得留，因为顺带修掉了「空白签名被记成已签名」这个真 bug。

---

## 仓库整理

- 仓库从 `~/Downloads/mkr-git` 移到 **`~/Projects/mkr`**
- 使用说明书（PDF + HTML + 15 张截图）进仓库 —— 原本只在一份本地副本里，不在 git 也不在 GitHub，clone 下来一份文档都没有
- demo 的 2 个本地 commit 打成 bundle 存到 `~/Projects/mkr-owner-game-demo-history.bundle`
- 三份旧副本移到 `~/Downloads/_mkr-old-copies-safe-to-delete/`（781M，未删除，等确认）
